Public Class Main

    Public Shared Sub Main()
        'Ejecutamos nuestra aplicación
        'Application.EnableVisualStyles()
        'Dim loFrmLogin As New FrmLogin()
        'loFrmLogin.ShowDialog()
        'Dim Resultado As DialogResult
        'Resultado = loFrmLogin.DialogResult
        'If Resultado = DialogResult.OK Then
        '    Dim loForm As New FrmMainMDI
        '    Application.Run(loForm)
        '    ' appsust.main.Main()
        '    'ElseIf Resultado = DialogResult.OK And x = 1 Then
        '    '    Dim loForm1 As New Form1
        '    '    Application.Run(loForm1)
        'End If
    End Sub


End Class
